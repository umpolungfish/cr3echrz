#!/bin/bash
# p3 — IMASM Theorem Bootstrap Launcher
# Usage:  ./p3 threebody
#         ./p3 collatz 27
#         ./p3 goldbach 100
#         ./p3 --list
cd "$(dirname "$0")" && exec python3 main.py "$@"
